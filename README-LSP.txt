# Project LSP — Overlay (Terima Jadi)

Ini paket siap tempel untuk folder Laravel-mu (`project-LSP`). Cukup ekstrak ke root proyek lalu jalankan:

  php artisan storage:link
  php artisan migrate --seed
  php artisan serve

Sudah termasuk:
- app/Models, app/Http/Controllers
- database/migrations & seeders
- resources/views (arsip, kategori, about) + layout
- routes/web.php
- public/css/wireframe.css
- database/database.sqlite (kosong)
- public/img/foto-anda.jpg (placeholder — ganti dengan fotomu)

Akses:
  /letters, /categories, /about
